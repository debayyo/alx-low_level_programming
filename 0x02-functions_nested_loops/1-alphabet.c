#include "stdio.h"

/** 
 * *print_alphabet - prints the alphabet, in lowercase, followed by a new line.
 */
void print_alphabet(void)
{
	char c;

	for (c = 'a'; ch <= 'z'; ch++)
		putchar(c);
	putchar('\n');
}
